{
  "name": "<%= appname %>",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "gulp": "^3.9.0",
    "which": "^1.1.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"<% if(includePhoenix) { %>,
    "postinstall": "cd ./assets/src && npm install"<% } %>
  },
  "author": "",
  "license": "ISC"
}